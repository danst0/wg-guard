#!/usr/bin/env bash
# Mock fuer install(1) – ignoriert Besitzerwechsel, die ohne root scheitern.
set -uo pipefail
ARGS=()
while [ "$#" -gt 0 ]; do
	case "$1" in
	-o|-g) shift 2 ;;
	*) ARGS+=("$1"); shift ;;
	esac
done
exec /usr/bin/install "${ARGS[@]}"
